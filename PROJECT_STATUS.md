# PROJECT_STATUS.md — 현재 진행 상황

**최종 갱신**: 2026-08-27 (관리자 화면 개편 · 관할지역)

> ✅ **열려 있던 확인 항목이 모두 닫혔다** (2026-08-27, 사용자 확인).
> 부계정 재로그인 · 관할권역 · 에이전시 칼럼 · 이미지 저장 · 배포 전부 정상.
> `main` 과 `origin/main` 은 동기화돼 있고 미커밋 변경도 없다.
**배포 URL**: https://hotel-roomcheck-356950571433.asia-southeast1.run.app/

> 새 세션은 `CLAUDE.md`(변하지 않는 개발 원칙)를 먼저 읽고, 이 문서의
> **6. 다음에 할 일**부터 이어서 진행한다. 기존 기능을 임의로 지우거나 바꾸지 않는다.

---

## 1. 완료된 기능

### 호텔 · 룸타입 · 지역 입력
- 지역을 바꾸면 호텔·룸타입이 자동으로 비워진다 (기본·추가·추천 세 곳 모두)
- 호텔 이름 빨리 찾기 — 한글·영문, 띄어쓰기·대소문자 무시. 한 글자부터 걸러 준다
- ▾ 를 누르면 이미 고른 값과 무관하게 등록된 전체 목록이 다시 열린다
- 목록은 화면 언어 기준으로 정렬 (한글 ㄱㄴㄷ / 영문 A→Z). 룸타입은 API `sort` 순서를 지킨다
- 호텔을 고르면 지역이 자동 선택되고 룸타입을 받아온다
- 목록 응답의 `room_types` 를 사용 — 있으면 상세 API 를 부르지 않는다

### 언어
- `isEN()` 이 역할이 아니라 **실제로 고른 언어**를 따른다
- 지역·호텔·룸타입 모두 언어를 따르고, 한글이 없으면 영어로 내린다
- 지역 약어(`PK`, `KL`, `KI-PK`)는 어떤 경우에도 노출되지 않는다

### 에이전트 · 담당자
- 에이전트를 고르면 그 에이전시의 담당자만 나온다 (에이전시 상세 API 의 `managers`)
- `AG_DEL_YN='Y'` 담당자는 제외. 목록에 없는 사람은 직접 입력 가능
- API 실패 시 기존 입력 이력으로 폴백

### 관할권역 (중요 — 실제 장애였다)
서버가 지역으로 요청을 걸렀는데 **요청에 지역을 넣는 코드가 없어** 값이 늘 비어 있었다.
그 결과 직원은 **자기가 만든 요청만** 볼 수 있었다 (확인자에게 에이전트 요청이 안 넘어감).
호텔 지역에서 권역을 뽑아내도록 고쳤고, 읽을 때 계산하므로 기존 요청도 살아난다.

### 알림
- 조건에 맞는 직원 수만큼 중복 삽입되던 것을 권역별 1건으로
- `notifications.zone` 칼럼 추가 — 자기 권역 알림만 받는다

### requests 테이블 에이전시 칼럼 (2026-08-25)
```
agency_idx          BIGINT NULL        요청을 넣은 부계정 idx
agency_name         VARCHAR(120) NULL  그 사람 이름       ← 저장 시점 스냅샷
agency_parent_idx   BIGINT NULL        소속 에이전시 idx
agency_parent_name  VARCHAR(190) NULL  에이전시 이름      ← 저장 시점 스냅샷
```
- **새 요청에만 채운다.** 기존 요청은 비워 둔다 (사용자 결정)
- 부계정 직접 등록은 로그인 세션의 4개 값으로 저장하고, 직원의 대신 등록만 브라우저 선택값 사용
- `currentUser()` SELECT 에 `agency_parent_idx` / `agency_kind` 를 추가해야 했다 (빠져 있었음)
- 부계정 로그인 API의 `parent_agent_name`을 `agency` 세션에 함께 보관
- 로그인한 부계정은 화면 우측 상단에 `parent_agent_name · 사용자명`으로 표시

### 부계정 로그인 세션 (2026-08-26)
- 니르바나 로그인 응답의 `idx` · `name` · `parent_idx` · `parent_agent_name`을 세션에 보관
- 보조 필드 `kind` · `login_id` · `nickname`도 기존처럼 세션에 함께 보관
- `api/me`의 `user.agency`로 프론트에 전달하며 비밀번호는 세션·응답·DB 어디에도 저장하지 않음
- 에이전트가 직접 요청을 등록하면 서버가 브라우저 입력값보다 로그인 세션을 우선하여
  `agency_idx` · `agency_name` · `agency_parent_idx` · `agency_parent_name`에 스냅샷 저장
- 직원이 대신 등록할 때만 화면에서 선택한 에이전시 값을 사용
- 변경 전 로그인한 세션에는 새 필드가 없으므로 적용 확인 시 로그아웃 후 다시 로그인해야 함

### 화면
- 모바일 분기점 400px → 440px. 아이폰 에어(402px)가 데스크톱 설정을 받던 문제
- 좌우로 버려지는 폭 64px → 32px, 글씨 확대 (입력 15px / 라벨 11.5px)
- 라벨의 `(선택·입력)` 안내 문구 제거
- 목록 열 때 화면이 왼쪽으로 쏠리던 문제 (아이폰 사파리)
- 비밀번호 안내를 니르바나 재발급 방식으로 (한/영/태)
- 0바이트로 깨져 있던 `logo.png` 복구

### 저장소 정리
- `private/` 로 개인 작업 메모 분리 (git 제외)
- `.gitignore` 신규 생성 — sqlite · `.env` · 모든 백업 파일
- 백업 파일 15개를 git 추적에서 제외 (파일은 디스크에 남음)

### 저장소 · 배포 이미지 정리 (2026-08-25)
`Dockerfile` 이 `COPY . /var/www/html/` 로 폴더 전체를 복사하는데 `.dockerignore` 가
`data/` 한 줄뿐이라, 쓰지 않는 파일이 전부 이미지에 실려 나가고 있었다.

- **`server-laravel`, `server-laravel.backup.1786125886` 제거 — 합계 178MB**
  현재 구조는 PHP 8.4 + Apache 이고 참조하는 코드가 없다. 매 배포마다 실려 나갔다
- **`live/` 제거** — 같은 GitHub 저장소를 폴더 안에 한 번 더 clone 한 중첩 저장소(13MB).
  `.gitmodules` 매핑이 없어 서브모듈도 아니었다. 마지막 fetch 가 8/20 이라 46커밋
  뒤처져 있었고, 안에 있던 미커밋 변경은 이후 `server/public/app.js` 에 다시 반영된
  내용임을 줄 단위로 대조해 확인했다. **이로써 이 폴더의 git 저장소는 하나가 되었다**
- **`OLD/` 아카이브 제거** (924KB) — 루트 `public`·`src` 를 옮겨둔 것. 참조 없음
- **`server/mock_api.php`, `server/mock_agency.php` 제거** — 테스트용 모의 API. 참조 없음
- **`server/public/` 잔재 17개 제거** (1.1MB) — `app.js.bak`·`.orig`·`.rej` 등 백업 14개,
  `style.css.backup`, `patch_editable.py`, `.DS_Store`
- **`텔레그램 ID 권한 자료.xlsx` → `private/`** — 부계정 권한 자료라 저장소에 두지 않는다
- **`.dockerignore` 2줄 → 67줄** — 아카이브·중첩저장소·개인메모·백업·저장소메타·문서 제외

지운 파일은 전부 `_to_delete/` 로 옮겨두었다 (약 190MB). 확인 후 폴더째 버리면 된다.
배포 동작 변화는 없다 — `server/public` 23개 파일과 `server/src` 는 그대로다.

### 견적 이미지 저장 (2026-08-26)
- **누르는 즉시 내려받던 것을 미리보기 → 저장 확인으로 바꿨다.** 무엇이 저장되는지
  보고 고를 수 있다. 아이폰 사파리는 다운로드가 막혀 있어 눌러도 아무 일도 안 일어난
  것처럼 보였는데, 이제 «이미지를 길게 눌러 사진에 추가» 안내가 뜨고 창이 열려 있다
- **파일 이름에 내용을 담는다.** `견적.png` → `견적 (1)` → `견적 (10)` 이던 것을
  `마이 카오락 비치 리조트 3박+로빈슨 카오락 2박 08.30~09.04 견적서 08.26.png` 로.
  내부 공유용은 끝에 `(내부)`. 호텔이 많아 길어지면 호텔 목록만 줄이고 기간·발행일은 남긴다
- **색 평탄화** — 뽑기 직전에 사본의 색을 전부 `rgb/rgba` 로 바꾼다. 3절 참조
- **캔버스 크기 한계 대응** — 내용이 길면 배율을 낮춰서라도 한 장으로 뽑는다
  (아이폰 사파리는 한 변 4096px · 넓이 약 1,670만 px 제한)
- **실패하면 사유를 화면에 그대로 띄운다** (7초). `console.error` 에도 폭·높이·배율과 함께 남긴다

### 공유 링크 단축 (2026-08-26)
「내부 공유용 · 링크 복사」가 요청 전체를 base64 로 URL 에 담아 **2,046자**였다 —
카톡에 붙일 수 없었다. 복사 시점 내용을 서버에 저장하고 짧은 코드만 주소에 담는다.

```
.../check.html#s=20260826-A00001-R78Q          94자 (95% 감소)
                 └날짜┘ └요청번호┘ └랜덤4자┘
```

- 날짜·번호는 링크만 봐도 언제 무슨 요청인지 알라고 넣는다
- **뒤 4자가 자물쇠다.** 없으면 번호만 하나 올려 남의 요청을 열 수 있다 (A00001 → A00002)
- **스냅샷이라 한 번 만들어지면 바뀌지 않는다** — 요청을 나중에 수정해도 링크는 그대로.
  견적을 보낸 뒤 금액이 바뀌면 분쟁이 되므로 고정이 맞다
- 같은 요청을 또 복사하면 코드가 새로 생기고 이전 링크도 계속 산다. 만료 없음 (사용자 결정)
- 원가(`ws` 의 `price`)는 가리지 않고 그대로 담는다 — 내부 공유용이라는 사용자 결정
- 서버가 없거나 실패하면 예전 긴 링크로 물러난다. 예전 `#q=` 링크도 계속 열린다

### 발송 여부 표시 (2026-08-26)
버튼만 봐서는 이미 보냈는지 알 수 없어 같은 요청을 두 번 보내는 일이 있었다.

```
보내기 전   [룸체크 답변 보내기]        [견적 포함 발송]      브랜드색
보낸 뒤     [✅ 답변 발송됨 · 다시 보내기] [✅ 견적 발송됨]     회색
            답변 발송 8.26 10:10 · 심은선 | 견적 발송 8.26 11:10 · 심은선
```

보낸 뒤에도 누를 수 있다. 색은 회색 계열만 쓴다 — 상태색은 상태에만 (9절).

### 호텔 전화번호 분리 (2026-08-26)
니르바나 호텔 API 의 `telnumber` 가 `"076-584150 , 076-584-199"` 처럼 여러 번호를
한 문자열로 보낸다. 주소록에 한 건으로 넣고 있어서 화면에 통째로 보이고,
📞 링크가 `tel:076584150076584199` 가 되어 **전화가 걸리지 않았다.**

- `splitPhones()` 구분자 `, ; / | ·` 줄바꿈으로 나눈다. 숫자 5자리 미만은 번호로 안 본다
- `phoneList()` 주소록에서 꺼낼 때 갈라 준다 — **기존 저장 데이터를 고칠 필요가 없다**
- `telHref()` tel: 링크는 첫 번호만 쓴다
- 호텔1·추가 호텔 양쪽 적용. 화면엔 하나만 보이고 나머지는 드롭다운

### 관리자 계정 목록 (2026-08-26)
```
전  정애리 · 에이전트 / agency-12@agency.local
후  정애리 · Stay N Joy / 언어 한국어 · 가입 2026.08.01 · 아이디 ari
```
- `users.agency_parent_name` 칼럼 추가. 부계정 로그인 때마다 갱신한다
- **기존 계정은 비어 있다 — 그 부계정이 한 번 로그인해야 채워진다**
- `@agency.local` 합성 이메일은 화면에서 뺀다 (실제로 쓸 수 없는 주소라 오해를 부른다).
  대신 로그인 아이디를 적는다. 진짜 이메일은 그대로 보인다

### 견적서 카드 날짜줄 (2026-08-26)
`3박`·`2박` 배지가 박스 밖으로 나가던 문제. 날짜가 `nowrap` 이고 배지가 `flex:0 0 auto` 라
좁은 폭에서 합이 박스를 넘었다 (360px 58px · 390px 32px). 날짜를 줄바꿈되게 풀고 배지는
오른쪽에 붙인다. 호텔 이름이 글자 중간에서 잘리던 것도 `word-break:keep-all` 로 고쳤다.

### 관리자 화면 개편 (2026-08-27)
시안 4가지를 만들어 C안(계층)에 검색을 얹는 것으로 정했다.

**에이전트 관리 — 새 화면**  상단 메뉴가 `통계 / 에이전트 / 직원 계정 / 휴무일` 이 됐다.
- 에이전시 회사로 묶고 그 안에 부계정(담당자)을 넣는다. 회사명은 `users.agency_parent_name`
- 회사명이 없는 계정은 **「(회사 미지정)」**으로 모아 맨 아래. 그 부계정이 한 번 로그인해야 채워진다
- **검색** — 이름·회사·아이디 셋 다 찾는다. 찾으면 회사가 자동으로 펴지고 맞은 글자에 표시.
  회사명이 걸리면 그 회사 담당자가 모두 나온다
- **신규 대기** 건수를 위쪽 띠로 보여 준다 (검색 중에는 숨긴다)
- **정렬** — 이름순. 한글 ㄱㄴㄷ 먼저, 그다음 영문 A·B·C.
  `app.js` 의 `nameSort` 와 같은 방식 (admin.html 은 app.js 를 안 불러와 따로 뒀다)
- 전에 「전체 계정」의 에이전시 탭에서 하던 일이 전부 여기로 옮겨졌다 (빠진 기능 없음)

**직원 계정**
- 역할을 두 버튼 세그먼트에서 **드롭다운**으로. 역할이 늘어도 줄이 안 감긴다
- 목록에서 에이전시 계정을 빼고 직원만 남겼다 (어디로 갔는지 안내 문구를 뒀다)

**비밀번호 재발급**  직원이 바꾼 비밀번호를 잊으면 아무도 모른다 — 저장된 것은 해시다.
되돌리는 대신 관리자가 임시 비밀번호를 새로 정해 전달하고 직원이 다시 바꾼다.
```
POST api/admin/reset-password   관리자만
  에이전트(부계정) 제외 — 그쪽은 니르바나에서만 발급
  최고관리자 계정은 본인만
  6자 미만 거절
```

### 관할지역 (2026-08-27)

```
카오락 + 푸켓   카오락 · 푸켓 · 크라비 · 사무이 · 방콕
방콕 + 파타야   방콕 · 파타야
```

- **방콕이 양쪽에 들어간다.** 방콕 호텔이 낀 요청은 두 팀 모두에게 보이고 알림도 양쪽에 간다.
  한 지역이 권역 하나가 아니게 되어 `regionZone()` → `regionZones()` 배열로 바꿨다
- 사무이를 표에 명시했다 (전에는 「표에 없는 지역」 규칙으로 우연히 카오락+푸켓에 갔다)
- 표에 없는 지역(치앙마이 등)은 카오락+푸켓 담당이 맡는다
- **관리자도 직원과 같은 규칙** — 관할지역에 걸친 요청 + 본인이 등록한 것.
  전에는 일반 관리자가 내부 목록에서 빠져 「본인 등록건」만 보였다 (버그였다).
  최고관리자는 전부 본다
- **정하는 것은 관리자다. 직원은 보기만 한다.**
  `api/me PATCH` 의 region 저장을 없앴다 — 화면에서 입력칸만 지우면 요청을 직접 보내
  바꿀 수 있으므로 서버에서 막았다. 변경 경로는 `POST api/admin/setregion` 하나뿐
- 회원정보 화면에서는 자기 관할을 **읽기 전용**으로 보여 주고 안내를 붙인다 (한/영/태)
- 계정 발행 폼에 관할 지역 드롭다운, 직원·관리자 카드에서 고르는 즉시 저장

### 상태 저장 규칙 (2026-08-25)
`CLAUDE.md` **9-11절** 신설. 이 프로젝트(`~/nirvana_project/hotel roomcheck`)에서
**"현재 상태 저장해"** 는 이 문서를 갱신하라는 뜻으로 확정했다. 7개 절 구조 유지,
기존 내용 삭제 금지, 틀린 기록은 3절에 정정 추가, 갱신 전 백업, `docs:` 커밋.

---

## 2. 수정한 파일

| 파일 | 내용 |
|------|------|
| `server/public/app.js` | 위 프론트 기능 + 소속명 헤더 표시·요청 에이전시 스냅샷 생성 |
| `server/public/i18n.js` | 라벨·안내 문구 (ko/en/th) |
| `server/public/style.css` | 모바일 분기점·글씨, `.hfind` 목록 |
| `server/public/login.html`, `profile.html` | 비밀번호 안내 |
| `server/src/lib.php` | 권역 계산, `currentUser()` SELECT 보강 |
| `server/src/events.php` | 알림 권역·중복 |
| `server/src/router.php` | 알림 읽기 권역 필터, requests 칼럼을 부계정 세션 기준으로 저장 |
| `server/src/agency.php` | 부계정 로그인 세션에 `name`·`parent_agent_name` 보관 |
| `server/src/db.php` | `notifications.zone`, `requests` 칼럼 4개 |
| `docs/룸첵-작동원리.html` | 작동 원리 설명서 (도표 7개) |
| `.dockerignore` | 2줄 → 67줄. 배포 이미지에 들어갈 것만 남긴다 |
| `CLAUDE.md` | 9-11절 — "현재 상태 저장해" 의 뜻과 갱신 규칙 |
| `PROJECT_STATUS.md` | 1·2·3·5·6·7절 갱신 (저장소 정리 · 정정 · 검증) |
| `server/public/admin.html` | 계정 목록 회사명 표시 · 합성 이메일 숨김 (2026-08-26) |
| `server/src/db.php` | `shares` 테이블 · `users.agency_parent_name` (2026-08-26) |
| `server/src/agency.php` | 로그인 시 소속 회사명 저장·갱신 (2026-08-26) |
| `server/src/router.php` | `POST/GET api/share` · `admin/users` 에 회사명·아이디 (2026-08-26)<br>`admin/reset-password` · `admin/setregion` · `me PATCH` 에서 region 제거 (2026-08-27) |
| `server/src/lib.php` | `regionZones()` 배열화 · 방콕 양쪽 · 사무이 · 관리자 권역 적용 (2026-08-27) |
| `server/public/profile.html` | 관할지역을 읽기 전용 표시로 (2026-08-27) |

---

## 3. 아직 해결하지 못한 것

### 확인이 필요한 것 (사용자)

**지금은 없다.** 열려 있던 항목이 2026-08-27 에 모두 닫혔다 (아래 참조).

### 확인 끝난 것

**운영 확인 완료 (2026-08-27, 사용자 확인)** — 8/25 부터 열려 있던 4건
- ~~확인자에게 에이전트 요청이 뜨는지~~ → 정상
- ~~에이전트로 로그인했을 때 다른 회사 요청이 보이는지~~ → 정상
- ~~실 부계정 재로그인 후 헤더에 `parent_agent_name` 표시~~ → 정상.
  관리자 목록의 회사명도 이때 채워진다
- ~~새 에이전트 요청의 에이전시 칼럼 4개가 운영 DB 에 저장되는지~~ → 정상

**배포 확인 완료 (2026-08-27)** — 8/26 에 열려 있던 `209f089` 배포 건.
그 뒤 `20260827a·b·c` 까지 나갔고 전부 반영됐다.
다음에 배포 여부를 확인할 때는 이 명령을 쓴다 (버전 숫자만 그때 것으로 바꾼다).
```
curl -s <배포URL>/admin.html | grep -o 'style.css?v=[^"]*' | head -1
```


**`DB_DSN` 은 MySQL 로 설정돼 있다 (2026-08-26 확인).** 오래 열려 있던 항목이다.

```
DB_DSN = mysql:unix_socket=/cloudsql/hotel-roomcheck:asia-southeast1:rc-sql
         ;dbname=roomcheck;charset=utf8mb4
DB_USER = rcuser
DB_PASS = (Cloud Run 환경변수에 설정됨)
```

운영 데이터는 Cloud SQL(MySQL)에 있다. **배포마다 초기화되는 상황이 아니다.**
`server/src/db.php` 의 SQLite 는 `DB_DSN` 이 없을 때만 쓰는 폴백이고 운영에서는 쓰이지 않는다.

### ✅ 해결 — 「내부 공유용 → 이미지」 (2026-08-26)

운영에서 정상 동작 확인 (사용자 확인). 세 번에 걸쳐 고쳤다.

| 시도 | 커밋 | 내용 | 결과 |
|---|---|---|---|
| 1차 | `037404e` | 산출 내역 표의 `color-mix` 4곳을 고정색으로 | 실패 |
| 2차 | `b97aca8` | `flattenColors()` — 뽑기 직전 사본의 색을 전부 `rgb/rgba` 로 | 실패 |
| 3차 | `3abd23a` | 실패 사유 화면 표시 + 캔버스 크기 한계 대응 (배율 자동 조정) | **해결** |

원인 — 크롬은 `color-mix()` 를 `color(srgb ...)` 로 계산해 내놓고 html2canvas 는
그 표기를 못 읽어 그림 전체를 포기한다. 이 프로젝트에서 네 번 밟았다
(`.qc-dbar` 8/1, `.ntitem` 8/1, `.qbd-*` 8/26, 전면 평탄화 8/26).
1·2차로 색은 정리됐지만 그것만으로는 부족했고, 3차의 **캔버스 크기 한계 대응**
(아이폰 사파리 한 변 4096px · 넓이 약 1,670만 px — 호텔이 여러 개면 세로로 길어져 넘는다)
까지 들어가서 해결됐다.

**이 건에서 배운 것 — 다음에 같은 실수를 하지 않기 위해**
1. 색 문제를 확인하고도 두 번 더 실패했다. **한 가지 원인을 찾았다고 그것이 유일한
   원인이라고 단정하면 안 된다.**
2. 테스트 요청에 추가·추천 호텔이 없었고 세로 길이도 짧아, 컨테이너 검증이
   두 번 다 통과해 버렸다. **검증 데이터가 운영과 다르면 검증이 아니다.**
3. 결국 「실패 사유를 화면에 띄우기」가 가장 빨리 통했다.
   원인을 모르면 추측해서 고치지 말고 **관측 수단부터 넣는다.**

### 구조 문제 (미수정)
- **에이전트 목록 거르기가 사람 이름과 회사 이름을 맞춰보는 방식이다.** 담당자가 이직하면
  깨진다. 회사(`parent_idx`) 기준으로 바꾸고 **서버로 옮겨야 한다** — 지금은 브라우저에서만
  거르고 서버는 모든 에이전트에게 모든 회사 요청을 내려보낸다
- **금액 가림도 화면 단에서만 이뤄진다.** 서버는 에이전트에게도 워크시트 전체를 보낸다
- **부분 답변 알림 개수가 항상 `?`** — 브라우저가 `_doneCount` 를 안 보낸다 (한 줄 수정)
- 쓰이지 않는 잔재: `quoteAsk` · `quoteOnly` · `row.subOptions` · `req.checkerAddedHotels`

### 니르바나 회신 대기
- 부계정 비밀번호 **자동 발급 · 변경 API**
- **부계정 목록 API** (지금은 로그인한 적 있는 부계정만 알 수 있다)
- 담당자 목록 API (있으면 담당자→에이전시 자동 채움이 훨씬 간단해진다)

### 검증 못 한 것
컨테이너·Mac 원격 셸 모두 `nirvana835.mycafe24.com` 접근이 프록시에서 막혀 있다.
**실 API 응답 필드명은 문서와 관리 화면 스크린샷 기준**이며, 실물로 확인하지 못했다.
Mac 터미널에서 로컬 실행하면 실 API 로 확인 가능하다.

### 이 문서의 이전 기록 정정 (2026-08-25)
6절에 **"백업 파일이 git 에서만 빠지고 배포 이미지에는 들어간다"** 고 적혀 있었으나
**사실이 아니었다.** 원격 저장소를 clone 해 확인한 결과 `server/public/` 에 백업 파일은
0개였다. 이미 `.gitignore` 로 빠져 있었고, GitHub Actions 는 checkout 한 코드로 빌드하므로
이미지에 들어간 적이 없다. **웹으로 소스가 새어나가는 상황은 없었다.**

다만 Mac 에서 로컬로 `docker build .` 할 때는 실제로 들어가므로 `.dockerignore` 보강은
유효하다. 그리고 `server-laravel` 178MB 는 저장소에 실제로 있었으므로 **이쪽이 진짜
낭비였다.**

### 작업 환경 — git 잠금 파일 (2026-08-25)
Cowork 원격 세션에서 이 폴더에 git 명령을 실행하면 `.git/index.lock` 과 `HEAD.lock` 이
남아 다음 명령을 막는다. 세션이 마운트된 폴더에서 **파일을 지울 권한이 없어** git 이
자기 잠금 파일을 정리하지 못하기 때문이다. `_to_delete/gitlocks_*` 가 그 잔재다.

**Mac 터미널에서 직접 실행할 때는 생기지 않는다.** 이 폴더의 git 작업은 Mac 터미널에서
하는 편이 안정적이다.

### 이 문서의 push 기록 정정 (2026-08-26)
이전 기록에는 커밋 5개가 미push라고 되어 있었으나, `git status` 확인 결과
`main...origin/main`으로 동기화되어 있다. 부계정 개선 커밋 `8c4751f`, `b62188e`도
현재 `origin/main`에 포함되어 있다.

---

## 4. 현재 확정된 계산 공식

- 신규 견적 환율 `QRATE = 45`. 기존 견적은 저장값 유지
- 호텔 요금 = 구간별 선택 호텔의 일자별 `price` 합
- 워크시트 키: `rowId|iso` · `rowId_chk_ci|iso` · `rec_recId|iso`
- 총액(원) = (호텔 ฿ + 옵션 ฿) × 환율 + 추가항목(원)
- 1인 만원 = 총액 ÷ 인원 ÷ 10,000
- 진행률 분모 = 기본 호텔 + 추가 호텔. **추천 호텔은 제외**

---

## 5. 테스트 결과

Playwright + PHP 8.4 로 Cloud Run 과 같은 구조에서 검증. **JS 오류 0건.**

- 권역: 카오락/방콕/카오락+파타야/사무이 4건 등록 후
  krabi 담당 → 1·3·4 / bangkok 담당 → 2·3 / 미설정 → 전부
- requests 칼럼: 부계정 직접 등록 4개 다 채워짐, 요청자 대행 시 parent 만 채워짐,
  브라우저가 안 보내도 세션에서 채워짐
- 룸타입: 원본 7건(중복·삭제·비노출·비활성 포함) → 화면 3건, sort 순서 유지
- 모바일 360/390/402/430px 두 줄 라벨 없음, 가로 넘침 0px
- 전 페이지 HTTP 200 (admin 403 은 부계정 권한 차단으로 정상)

### 부계정 세션·요청 저장 변경 검증 (2026-08-26)
- `php -l server/src/agency.php` 통과
- `php -l server/src/router.php` 통과
- `node --check server/public/app.js` 통과
- IDE 린트 오류 0건
- 실 니르바나 API 로그인과 운영 DB INSERT는 아직 검증하지 못함

### 2026-08-26 작업 검증
컨테이너에 **PHP 8.4 실서버 + Playwright 실브라우저**를 띄워 돌렸다.

- **공유 링크** 서버 12건 — 정상 읽기 · 뒤4자 틀림 404 · 번호 바꿔치기 404 ·
  날짜 바꾸기 404 · 형식 오류 400 · SQL 주입 400 · 미로그인 생성 401 · 빈 요청 400 ·
  두 번 복사 시 코드 다름 · 이전 링크 유지.
  **스냅샷 고정** — 원본 견적을 58→99 로 바꿔도 링크는 58 그대로.
  브라우저 7건 — 로그인 → 링크 열기 → 목록 반영 → 주소 정리 → 없는 링크 무해 →
  예전 `#q=` 링크 정상
- **전화번호** `splitPhones` 9건 · `telHref` 3건 — 쉼표·슬래시·세미콜론·중점·줄바꿈·
  중복·빈값·메모·국가번호. `phoneList` 실데이터 형태 확인
- **이미지 미리보기** 데스크톱·아이폰(UA+터치) 26건 — 창 표시·제목·파일명·버튼·취소·
  바깥클릭·중복호출 1개 유지·다운로드 파일명·아이폰 안내 후 창 유지
- **파일명** 15건 — 기본·내부·호텔1개·호텔없음·금지문자·호텔8개 길이제한·실제 버튼 2개
- **발송 여부 표시** 16건 — 3단계 문구·색 변화, 버튼에 상태색 없음, 발송 후 클릭 가능,
  360·390·430px 한 줄·가로 넘침 0px
- **날짜줄 넘침** 360/390/430px × 2구간 — 전부 0px (전에는 58/32/3px)
- **색 평탄화** `color-mix` 를 쓰는 클래스를 전부 한 판에 깔고 —
  평탄화 전 실패, 평탄화 후 OK, 남은 신형 색 0건
- **관리자 목록** 8건 — 회사명 있음/없음 · 합성 이메일 숨김 · 실제 이메일 유지 ·
  로그인 아이디 표시 · 칼럼 자동 생성

⚠️ **모두 컨테이너 검증이다.** 「내부 공유용 이미지」는 컨테이너 검증을 두 번 통과하고도
운영에서 두 번 실패했다 — 테스트 요청에 추가·추천 호텔이 없었고 세로 길이도 짧았기 때문이다.
**앞으로 이미지 검증은 추가 호텔·추천 호텔이 있고 세로로 긴 요청을 반드시 포함한다.**
(현재는 해결됨 — 3절 참조)

### 저장소 정리 검증 (2026-08-25)
- `server/public` 23개 파일이 원격 저장소 목록과 일치. 배포 필수 파일 11종 존재 확인
- `server/` 는 자기완결 구조 확인 — `index.php` → `__DIR__.'/../src/router.php'`,
  `server/` 밖을 참조하는 코드 없음 (`../../` 참조 0건)
- `live` 최신 커밋 `1be0b13` 이 `origin/main` 의 조상임을 확인 (이미 GitHub 에 있음)
- `live` 미커밋 변경 384줄 중 288줄이 본체에 이미 존재. 나머지는 표시 방식 차이
- 루트 `public/app.js` 미커밋 변경 7건 중 6건이 배포본에 이미 반영 (3건은 주석까지 동일).
  남은 1건은 이후 `hgnum` 그룹 헤더 설계로 교체된 것

### 커밋 (2026-08-25, 전부 미push)
```
a84dd16  docs: "현재 상태 저장해" = PROJECT_STATUS.md 갱신 규칙 추가 (9-11)
08d7ada  docs: PROJECT_STATUS 갱신 — 저장소 · 배포 이미지 정리 반영
2da4b5c  chore: 텔레그램 ID 권한 자료.xlsx 를 private/ 로 이동
3c7e4f0  chore: OLD 아카이브 · live 중복 저장소 · 모의 API 파일 제거
814c1b7  chore: 배포 이미지에서 server-laravel 제거 + .dockerignore 보강
```
워킹트리는 깨끗하다. **push 후 배포 결과 확인이 남아 있다.**

### 추가 커밋 (2026-08-26, 원격 반영 확인)
```
8c4751f  feat: 부계정 로그인 API에 parent_agent_name 추가 및 세션 저장 방식 개선
b62188e  feat: 부계정 로그인 및 요청 처리 개선
```
`git status` 기준 기능 코드와 `origin/main`은 동기화되어 있다.

---

### 커밋 (2026-08-26)
```
209f089  feat: 관리자 계정 목록에 소속 회사명 표시 + 합성 이메일 숨김   배포됨 (8/27 확인)
3abd23a  fix: 이미지 실패 사유를 화면에 표시 + 캔버스 크기 한계 처리    배포됨 #201
b97aca8  fix: 이미지 저장 실패 — 색을 뽑기 직전에 전부 변환             배포됨 #200
037404e  fix: 내부 공유용 이미지 1차 + 견적서 파일명 + 날짜줄 넘침      배포됨 #199
d181d04  docs: DB_DSN 확인 완료 — 운영은 Cloud SQL(MySQL)
e85b7b3  feat: 공유 링크를 2,046자에서 94자로
eb04a30  feat: 답변·견적 발송 여부 표시 + style.css?? 오타 수정
c57801b  feat: 이미지 저장 전에 미리보기로 확인
7b3e666  docs: 세션 시작·종료 규칙 (CLAUDE.md 9-12)                    배포됨 #198
2434d6e  fix: 호텔 전화번호가 두 개 붙어 나오고 전화가 걸리지 않던 문제
```
`main` 과 `origin/main` 은 동기화돼 있다. **배포만 `3abd23a` 에서 멈춰 있다.**

---

### 2026-08-27 작업 검증
PHP 8.4 실서버 + Playwright. 에이전시 5곳·담당자 9명·직원 3명·부관리자 1명·요청 8건을 심고 확인.

- **에이전트 화면** 13건 — 메뉴·화면 전환·회사 이름순(가온여행→리조트온라인→ARI Travel→
  Stay N Joy→미지정)·담당자 이름순·집계·신규 띠·아코디언 펴고 접기
- **검색** 6건 — 이름·회사명(전원 표시)·아이디·없는 값·지우기·자동 펼침·글자 표시
- **직원 계정** 5건 — 드롭다운·선택지·안내문구·에이전시 제외
- **비밀번호 재발급** 4건 — 입력칸·6자 미만 거절·성공·**새 비밀번호로 실제 로그인 확인**
- **권역** 지역→권역 14가지 · 요청단위 8가지. 계정별로 보이는 요청을 대조

```
카오락담당(krabi)   카오락 · 방콕 · 사무이 · 치앙마이 · 전체 · 카오락+방콕
                    + 파타야(본인 등록)        ← 파타야는 안 보인다
방콕담당(bangkok)   방콕 · 파타야 · 카오락+방콕
부관리자(bangkok)   방콕담당과 동일             ← 관리자도 같은 규칙
제한없음 / 최고관리자  전부
```

- **관할지역 잠금** — 직원 계정으로 `PATCH /api/me {region:bangkok, phone:010}` →
  관할 `krabi` 그대로, 연락처는 저장됨 (다른 항목은 정상)
- **버튼 줄바꿈** 360·390·430px — 글자 잘림 0 · 가로 넘침 0

### 커밋 (2026-08-27)
```
cc2aeab  change: 관할지역은 관리자만 정한다 — 직원은 보기만
28ed8fc  feat: 직원 관할지역을 관리자가 정한다 + 방콕을 두 권역 모두에
024c475  feat: 관리자 화면 개편 — 에이전트 관리 신설 + 직원 계정 정리 + 비밀번호 재발급
```
전부 원격 반영·배포 완료.

---

## 6. 다음에 할 일

1. **에이전트 목록 거르기를 회사 기준으로 바꾸고 서버로 이동** — 위 "구조 문제" 첫 항목.
   운영 확인 결과에 따라 방향이 갈린다
2. **금액 가림을 서버로 이동** — 서버가 에이전트에게 보낼 때 `ws` 의 `price` 를 제거
3. **부분 답변 알림 개수** — 브라우저가 `_doneCount` 를 함께 보내도록 한 줄 추가
4. 담당자 → 에이전시 자동 채움 (에이전시 개수·캐시 기간·동명이인 처리 결정 필요)
5. 잔재 필드 정리 — `quoteAsk` · `quoteOnly` · `row.subOptions` · `req.checkerAddedHotels`
6. **루트 `public/` 과 `src/` 를 어떻게 할지 결정** — 배포되지 않는데 저장소에 남아 있다.
   `OLD/` 로 옮겼어야 할 것이 되돌아온 상태로 보인다. 지울지 되살릴지 판단 필요
7. ~~`Dockerfile` 의 평문 비밀값 처리~~ → **이 문서의 할 일에서 내린다 (2026-08-27).**
   사용자가 다른 경로로 대처하기로 했다. 코드에는 그대로 남아 있으므로 상황만 적어 둔다 —
   `TELEGRAM_BOT_TOKEN` · `TELEGRAM_WEBHOOK_SECRET` · `CRON_KEY` · `ADMIN_PASSWORD` 가
   `ENV` 로 박혀 있고 커밋 이력에도 남아 있다. 나중에 저장소를 공개로 돌리거나
   외부에 넘길 일이 생기면 그때 다시 볼 것.
   (같은 성격 — 2026-08-26 대화창에 `DB_PASS` 가 평문으로 한 번 노출됐다)

### 완료 (2026-08-27)
- ~~관리자 화면 개편~~ → 에이전트 관리 신설 · 직원 계정 정리 · 비밀번호 재발급
- ~~직원 관할지역을 관리자가 정한다~~ → 발행 폼 + 카드에서 즉시 저장
- ~~권역 규칙 개정~~ → 방콕 양쪽 · 사무이 명시 · 관리자도 같은 규칙
- ~~직원이 자기 관할지역을 바꾸던 것~~ → 서버에서 막고 읽기 전용 표시로
- ~~열려 있던 운영 확인 4건 + 배포 확인~~ → 3절 「확인 끝난 것」 참조

### 완료 (2026-08-26)
- ~~견적 이미지 파일명~~ · ~~이미지 미리보기 후 저장~~ · ~~발송 여부 표시~~
- ~~공유 링크 단축 (2,046자 → 94자)~~ · ~~호텔 전화번호 분리~~
- ~~견적서 날짜줄 배지 넘침~~ · ~~`style.css??v=` 물음표 두 개~~
- ~~관리자 목록 회사명·합성 이메일~~ → 코드 완료, 배포 확인 남음
- ~~「내부 공유용 → 이미지」가 안 만들어지던 문제~~ → 3차 시도(`3abd23a`)에서 해결.
  운영 확인 완료. 경위와 교훈은 3절에 남겼다

### 완료 (2026-08-25)
- ~~`.dockerignore` 에 백업 파일 패턴 추가~~ → 1절 "저장소 · 배포 이미지 정리" 참조
- ~~저장소 2개(본체 + `live`) 정리~~ → 하나로 통합
- ~~`_to_delete/` 정리 대상 분류~~ → 약 190MB. 사용자가 확인 후 버리면 된다
- ~~미push 커밋 5개 push~~ → 2026-08-26 `main...origin/main` 동기화 확인

---

## 7. 절대로 변경하면 안 되는 것

`CLAUDE.md` 9절 참조. 요약하면:

- 상태 색 `--av` · `--rq` · `--so` 는 예약색 — 버튼·태그·탭에 쓰지 않는다
- `isEN()` 은 **고른 언어** 기준. 지역은 약어를 쓰지 않는다
- 추천 호텔 금액은 에이전트에게 보이지 않는다 (`costVisible()`)
- 추천 호텔은 진행률에서 제외 — 추가 호텔과 같은 배열에 섞지 않는다
- 워크시트 키 스킴, localStorage 키 `nirvana_roomcheck_v2`
- `requests` 의 `agency_name` / `agency_parent_name` 은 **저장 시점 스냅샷** — 갱신하지 않는다
- Cloud Run DocumentRoot 는 `server/public` (루트 `public/` 이 아니다)
- `server/.env` 는 커밋 금지
- **배포에 실제로 쓰이는 것은 `server/` 하나뿐이다** — `server/public`(웹 루트) +
  `server/src`(PHP). 루트 `public/`·`src/` 는 배포되지 않는다. 기능을 고칠 때
  **루트 쪽을 고치면 배포에 반영되지 않는다** (2026-08-25 실제로 그런 미커밋 변경이 있었다)
- `.dockerignore` 를 줄일 때는 `server/` 와 `Dockerfile` 이 제외되지 않는지 확인한다
- **이미지로 뽑히는 화면에는 `color-mix()` 를 쓰지 않는다.** 써야 한다면
  `flattenColors()` 가 처리하는지 반드시 확인한다 — 하나만 걸려도 그림 전체가 안 나온다
- **공유 링크(`shares`)의 `payload` 는 스냅샷이다.** 나중에 요청이 바뀌어도 갱신하지 않는다
- 파일·화면을 고칠 때 `server/public/*.html` 의 **캐시 버전(`?v=`)을 함께 올린다.**
  안 올리면 브라우저가 옛 파일을 계속 쓴다
- **관할지역은 관리자만 정한다.** 직원이 자기 것을 바꾸는 길을 다시 열지 않는다 —
  화면에서 감추는 것으로는 부족하고 서버(`api/me PATCH`)에서 막아야 한다
- **방콕은 두 권역 모두에 속한다.** 한 지역이 권역 하나라고 가정하는 코드를 쓰지 않는다
  (`regionZones()` 는 배열을 돌려준다)
- **에이전트·담당자 목록은 언제나 이름순** — 한글 ㄱㄴㄷ 먼저, 그다음 영문 A·B·C
- 에이전트 비밀번호는 니르바나에서만 발급한다. 관리자 화면에서 만들거나 바꾸지 않는다
